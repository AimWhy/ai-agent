# Agents.md

## Execution Rules

- After code changes, do not automatically run verification commands, tests, builds, dev servers, or browser checks unless the user explicitly asks for them.
- The user will manually verify UI and behavior after changes are complete.
